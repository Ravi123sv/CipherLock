name: CI (Web)

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  build-web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Enable pnpm
        run: corepack enable

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build cipherlock
        working-directory: artifacts/cipherlock
        env:
          PORT: "4173"
          BASE_PATH: "/"
        run: pnpm run build

